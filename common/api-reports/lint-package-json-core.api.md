## API Report File for "@lint-package-json/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { PackageJson } from 'read-pkg';

// @public
export interface Config<TStringRuleConfig, TRulesConfig extends RulesConfig<TStringRuleConfig>> {
    extends?: string | string[];
    rules?: TRulesConfig;
}

// @public
export const DEFAULT_CONFIG: LinterConfig;

// @public
export enum DependencyStrategy {
    any = "any",
    approximate = "approximate",
    compatible = "compatible",
    dotX = "dotX",
    exact = "exact",
    git = "git",
    github = "github",
    greater = "greater",
    greaterOrEqual = "greaterOrEqual",
    local = "local",
    pnpmWorkspace = "pnpmWorkspace",
    range = "range",
    smaller = "smaller",
    smallerOrEqual = "smallerOrEqual",
    tag = "tag",
    url = "url"
}

// @public
export const LINT_PACKAGE_JSON_RULES: LinterRule[];

// @public
export type LinterConfig = Required<Config<LinterStringRuleConfig, LinterRulesConfig>>;

// @public
export interface LinterRule {
    description: string;
    exec(context: LinterRuleContext): void;
    fixable: boolean;
    name: string;
}

// @public
export interface LinterRuleContext {
    config: LinterConfig;
    fix: boolean;
    pkg: PkgInfo;
    report(message: string): void;
}

// @public
export type LinterRulesConfig = Required<RulesConfig<LinterStringRuleConfig>>;

// @public
export type LinterStringRuleConfig = Required<StringRuleConfig<RegExp[], string[], Required<RuleConfig>>>;

// @public
export enum LintLevel {
    error = "error",
    info = "info",
    off = "off",
    warning = "warning"
}

// @public
export function lintPackageJson(input: readonly string[], fix?: boolean, configFile?: string): void;

// Warning: (ae-internal-missing-underscore) The name "loadConfig" should be prefixed with an underscore because the declaration is marked as @internal
//
// @internal
export function loadConfig(fileName?: string): LinterConfig;

// @public
export const NPM_DESCRIPTION_REGEX: RegExp;

// @public
export const NPM_NAME_REGEX: RegExp;

// @public
export const NPM_PEOPLE_REGEX: RegExp;

// @public
export interface PkgInfo {
    data: PackageJson;
    file: string;
}

// @public
export interface RuleConfig {
    // (undocumented)
    level?: LintLevel;
}

// @public
export interface RulesConfig<TStringRuleConfig> {
    author?: TStringRuleConfig;
    ban?: TStringRuleConfig[];
    contributors?: TStringRuleConfig;
    dependencyStrategy?: Record<string, DependencyStrategy>;
    description?: TStringRuleConfig;
    license?: LintLevel;
    name?: TStringRuleConfig;
    sort?: LintLevel | Partial<Record<string, LintLevel>>;
    version?: TStringRuleConfig;
}

// @public
export const SEMANTIC_VERSION_REGEX: RegExp;

// @public
export type StringRuleConfig<TRegExp, TValue, TRuleConfig extends RuleConfig> = TRuleConfig & {
    regex?: TRegExp;
    values?: TValue;
};

// Warning: (ae-internal-missing-underscore) The name "toLinterConfig" should be prefixed with an underscore because the declaration is marked as @internal
//
// @internal
export function toLinterConfig(config?: UserConfig): LinterConfig;

// @public
export type UserConfig = Config<UserStringRuleConfig, UserRulesConfig>;

// @public
export type UserRuleConfig<TRuleConfig extends RuleConfig> = LintLevel | TRuleConfig;

// @public
export type UserRulesConfig = RulesConfig<UserStringRuleConfig>;

// @public
export type UserStringRuleConfig = UserRuleConfig<StringRuleConfig<string | string[], string | string[], RuleConfig>>;


// (No @packageDocumentation comment for this package)

```
